# The UX audit of the story-first route

2026-09-12. An audit of the release branch from a user's seat, and the plan that
follows from it. Nothing here is implemented; the process comes first and the UI
is repaired after it. This document is the record so the work does not have to be
re-derived.

First read at `release/story-first` (`42f0ce9d`) on 2026-09-12 morning; every finding re-read against `main` at `5a1c5ce0` the same evening, after v0.77.0 had shipped. Every path is repo-relative.

## What prompted it

Running the branch end to end, as a person rather than as a test:

- A cut runs for minutes and says almost nothing about where it is. No estimate,
  no counts for the long phase, no sense that anything is happening.
- The CLI and the web UI report the same run through entirely separate plumbing,
  so neither can be the reference for the other.
- The result reads as a ranked list of stories, not as the video that was planned,
  and never shows every picture that was considered.
- Settings → People reloads the browser on every change.
- A Chrome tab grew during a cut until it blocked the machine.

The product has two audiences and serves neither cleanly. A scheduler runs many
memories unattended and has no face in the UI at all. A person building one
ad-hoc video gets a single Cut button with no way to see or change what the
editor decided.

## Decisions taken before this was written

| Question | Decision |
|---|---|
| Issue structure | One umbrella issue; #809 closes into it |
| Auto mode in the UI | A Suggestions page **and** a Runs page |
| Result view | Add a chronological storyboard, keep the ranked story |
| Release scope | All of it lands before the release. **Did not happen**: v0.77.0 shipped on 2026-09-12 with none of it; Part 2 now says what lands before 0.78 |

---

# Part 1 — What is wrong

## 1. Two unrelated progress systems

> Re-verified on main `5a1c5ce0`: `analysis/smart_pipeline.py:82,:91`, `operations/phases.py:10,:34`, `ui/pages/memory_run.py:52` (`CUT_PHASES`), `generate_progress.py:123`. Holds.

|  | Editorial cut (the new flow) | Render / export (old step 4) |
|---|---|---|
| Contract | `Callable[[dict], None]`, declared inline twice, no alias — `analysis/smart_pipeline.py:79`, `:88` | `Callable[[str, float, str], None]` plus `PhaseEvent` — `generate.py:174`, `operations/phases.py:33` |
| Carries | `phase_label`, `status`, `started_at`, `elapsed`. `indeterminate` is hard-coded `True` — `analysis/editorial_projection.py:48` | phase name, a real fraction, a message |
| Reaches the UI | **not through the callback** — the page polls two JSON files on disk each second | directly, by callback |

`operations/phases.py:10` holds the one canonical enumeration, `OperationalPhase`,
eight values with a stable `.order`. Three other vocabularies compete with it: the
UI's five-value `CUT_PHASES` with its own titles (`ui/pages/memory_run.py:52`), the
weight keys in `generate_progress.py:123`, and eleven free-text stage strings
scattered across the editorial modules.

Of the fifteen keys in `ui/pages/clip_pipeline.py:82`, thirteen are never written
by anything. The dict is never read for display either — `memory_run.py:324` reads
only `error`. In practice that callback is a cancellation probe.

## 2. The keystone: one line drops the numbers

> Re-verified on main `5a1c5ce0`: the drop moved to `analysis/editorial_runtime.py:525`; `editorial_projection.py:48` still hard-codes `indeterminate`; the prefix guess is `ui/pages/memory_run.py:178`; `cli/_pipeline_runner.py:253` still resets `total=None`; `cli/_live_display.py:339` still needs a total. Holds.

`analysis/editorial_runtime.py:485` computes real counts and hands them to a
writer that returns them, then throws them away:

```python
def progress(stage: str, done: int, total: int) -> None:
    if done not in {0, total} and done % batch_size:
        return
    if on_stage is not None:
        on_stage(live.publish(stage, done, total).stage_label)   # a string
```

`on_stage` is `Callable[[str], None]`, so `done` and `total` die here.
`EditorialStageReporter.__call__` (`analysis/editorial_projection.py:44`) then
hard-codes `"indeterminate": True` into the dict the CLI reads.

Every symptom follows from that seam:

- **The UI bar only appears for five stages.** `StageBar` renders only when the
  attempt's stage string matches `f"Preparing {label}: {done}/{total}"` exactly
  (written at `operations/cut_progress.py:49`, compared at `memory_run.py:165`).
  Only the per-asset preparation passes produce it.
- **The CLI's estimator is switched off exactly when it is needed.**
  `cli/_live_display.py:333` appends `~N remaining` only when the active task has a
  total. `_SourceProgressReporter._unbounded_stage` (`cli/_pipeline_runner.py:249`)
  calls `progress.reset(task, total=None)` because the status says indeterminate.
  The one estimator the product owns is disabled for the longest phase.
- **The phase is guessed from a string prefix** (`ui/pages/memory_run.py:170`):
  `startswith("Preparing")` means ANALYSIS, anything else means SELECTION. Rename a
  label and the phase rows silently mis-classify.

## 3. The long phase says four things

> Re-verified on main `5a1c5ce0`: the four strings are at `analysis/editorial_orchestration.py:166,:177,:188,:204`; `selection_stages` is written at `editorial_structure_planner.py:218,:520` and read by nothing. The retry is `llm_query.py:160-170`, and its observer reaches only `editorial_gateway.py:174` (a recorder). Holds; see Part 2 for the provider-down path.

The whole Editing stretch is four strings from `analysis/editorial_orchestration.py`
— `"Building editorial cards"` (`:166`), `"Editing the memory"` (`:177`),
`"Reading event evidence"` (`:188`), `"Reading the period account"` (`:204`).

`analysis/editorial_structure_planner.py:514` already maintains a `selection_stages`
dict with counts at `before_picture_review`, `after_shareability` and
`after_final_duplicate_review`, persists it, and nothing ever reads it.

A dead reading provider looks identical to a busy one: `analysis/llm_query.py`
retries three times with 2 s then 4 s of backoff before raising, saying nothing for
roughly six seconds. The retry is correct and must stay — a dropped connection once
killed a 26-minute run, and the ruling is that it is never fatal. What is missing is
that nobody is told.

## 4. A real bar bug on export

> Re-verified on main `5a1c5ce0`: `generate_progress.py:142`; emitters `generate_clips.py:244,:356` (`extract`), `generate_delivery.py:86` (`upload`), `generate.py:740` (`done`). Holds.

`generate_progress.py:111` promises the bar "only moves forward, never jumps
backward". Nothing enforces it, and `report()` maps an unknown phase to the whole
range:

```python
start, end = self._ranges.get(phase, (0.0, 1.0))   # generate_progress.py:142
```

`_ranges` holds `download`, `assembly`, `music`. Live call sites emit `"extract"`
(`generate_clips.py:244`), `"upload"` (`generate_delivery.py:86`) and `"done"`
(`generate.py:736`). Clip extraction sweeps the bar to full, then it snaps back.

## 5. The browser memory bomb

> Re-verified on main `5a1c5ce0`: `step2_helpers.py:29`, `step2_loading.py:403,:429` (`"preview"`), `clip_grid.py:352,:409`, `clip_review.py:54,:395`, `step4_export.py:70-72`, `cut_progress_view.py:28,:83` (the one bounded path). Holds.

Every image in the app is a base64 data URI of Immich's **preview**, not a
thumbnail. `get_thumbnail` returns `state.thumbnail_cache.get(asset_id, "preview")`
(`ui/pages/step2_helpers.py:23`), fetched as `size="preview"` at
`step2_loading.py:403` and `:447`. The codebase states the size itself:
`cache/thumbnail_cache.py:118` budgets "roughly 0.35 MB per candidate asset".

That JPEG becomes roughly half a megabyte of base64 in a DOM attribute plus about
six megabytes of decoded bitmap in the renderer, dropped into cells sized
`minmax(140px, 1fr)` (`clip_grid.py:352`). Two hundred cells is over a gigabyte.

Three multipliers on top:

- `ui/pages/clip_grid.py:409` `_render_paginated` **appends** each new page into the
  same container and never clears it.
- `ui/pages/clip_review.py:395` renders one `<video>` per selected clip with no
  pagination, and starts a preview loader for every row regardless of whether its
  expansion is open (`:283` controls only the fold). `ui.video(Path(...))` goes
  through `app.add_media_file`, which registers a route per file and never removes
  it. `tests/test_ui_media_routes.py` exists because this was already learned once.
- `ui/pages/step4_export.py:72` starts a five-second timer, never deactivates it,
  and its action is `ui.navigate.reload()`.

The live progress strip is the one place that got this right: it re-encodes to 96 px
at quality 70 before sending (`ui/pages/cut_progress_view.py:67`). The cut-progress
view is **not** the leak. It is bounded by construction — a fixed picture ring, a log
that deletes its own overflow labels, four arrivals per tick.

## 6. Clicking a thumbnail reloads the page

> Re-verified on main `5a1c5ce0`: `clip_grid.py:247,:310`; `settings_people.py:357,:410,:446`; the wizard picker `step1_people.py:117` still calls `apply` without navigating. Holds.

The compact grid's toggles end with `ui.navigate.to("/step2")` — `clip_grid.py:247`
for photos, `:310` for clips. That is a real browser navigation: it re-runs the page
builder, re-encodes every base64 thumbnail and collapses pagination back to the
first twenty. The card-view checkboxes (`clip_grid.py:171`, `:215`) perform the same
mutation for free.

The wizard's person picker does **not** reload; it writes to
`state.memory_preset_params` and calls `apply` (`step1_people.py:126`, `:211`,
`:269`). The reloads are on **Settings → People**: `ui/pages/settings_people.py:357`,
`:410`, `:446`, each re-fetching every face crop. That page also writes the whole
people YAML on every role change and every debounced keystroke (`:292`, `:307` →
`people/companion.py:112`, whose own docstring says it rewrites the entire
document), renders an unbounded roster (`:209`), and fetches thumbnails one request
per person (`:149`, docstring: "a household of seventy is seventy round trips").

## 7. The result is ranked, not the video

> Re-verified on main `5a1c5ce0`: the sort is `memory_story_data.py:185`; `timeline_plan` is set at `clip_pipeline.py:217,:422` and only passed on at `_step4_generate.py:248,:288`; `step2_review.py:107` still returns into the summary once `pipeline_result` exists. Holds.

```python
# ui/pages/memory_story_data.py:168
entries.sort(key=lambda entry: (_weight_rank(entry.weight), entry.day))
```

Stories come out ordered by editorial weight. The video is always chronological, so
the page never shows the cut in shot order. `state.timeline_plan` exists but is only
ever passed to the generator (`_step4_generate.py:248`, `:288`); nothing renders it.

Only granted carriers appear. `/step2` shows the full pool but hides itself the
moment a cut exists (`step2_review.py:107` returns early), so after a cut the grid is
unreachable until "Start Over".

This is also why a loose grouping reads as random: every carrier a story was granted
appears under one title, with nothing showing the days it actually spans.

## 8. A tick does not survive the cut

> Re-verified on main `5a1c5ce0`: `clip_pipeline.py:363` (exclusion only), `:424` (overwrite); `_recut` at `memory.py:57` still reuses `_start_over_selection` (`step2_review.py:42`), and `memory_run.py:389` still trusts `pool_loaded`. Holds.

`clip_pipeline.py:363` passes unticked media to the editor as
`owner_excluded_asset_ids`, so the negative works. There is no positive. After the
run, `_adopt_result` overwrites the user's choices:

```python
state.selected_clip_ids = {c.asset.id for c in result.selected_clips}  # clip_pipeline.py:424
```

"Add a picture the pipeline rejected" therefore means keep it ticked, press Cut and
hope. This is the same defect users reported as #778.

**Bug found while reading.** `_recut` (`ui/pages/memory.py:57`) reuses
`_start_over_selection` (`step2_review.py:42`), which empties `selected_clip_ids` but
leaves `state.clips` populated. `memory_run.py:388` then sees `pool_loaded` true,
skips the reload, and `_set_initial_selection` never re-runs — so "Cut again" runs
with zero eligible videos and whatever photos the previous cut left behind.

## 9. The decision log exists, is complete, and nothing renders it

> Re-verified on main `5a1c5ce0`: `cli/generate_options.py:223` arms it; `plan.private.json` is written by `editorial_structure_contract.py:204` and read only by `memory_story_data.py`. No UI module imports `selection_trace`. Holds.

`analysis/selection_trace.py` was written for exactly this question. It carries
`ClipStory` with `dropped_at` and `reason` (`:44`), `Stage.notes` mapping asset id to
reason (`:59`), `TraceDecision` (`analysis/editorial_contracts.py:236`),
`PassTrace.rejected` and `.unresolved` (`:254`), and a `ConservationCheck` proving
every input got exactly one fate. `Trace.report()` renders "why each clip is in the
cut" and "and why the rest are not". `Trace.story_of(asset_id)` answers the
single-picture question directly.

It is armed only by `generate --trace-selection <path>`
(`cli/generate_options.py:222`), writes two files nothing reads back, and when it is
off the reasons are never built (`:515`). No UI code mentions it.

`plan.private.json` is written by **every** run, carries a per-carrier `why` and the
moment grouping, and is read by exactly one module — the web UI's
`ui/pages/memory_story_data.py`. The CLI writes it on every run and cannot print it.

## 10. The scheduler has no face

> Re-verified on main `5a1c5ce0`: `ui/app.py:589` (`run_forever`), `trigger_api.py:38`, the stale step rule `ui/app.py:131`; `cli/auto_cmd.py:211-215` still offers only `--dry-run/--force/--cooldown/--upload/--quiet`; no page under `ui/pages/` mentions suggestions or runs. Holds.

`ui/app.py:589` starts `automation_scheduler.run_forever()` inside the web process.
`ui/trigger_api.py` exposes a parameterless `POST /api/trigger` that runs exactly the
decision `auto run` would make. `tracking/` keeps a `pipeline_runs` table with
status, `last_phase`, timestamps, delivery status and the Immich asset id.
`automation/status.py` has a read-only status contract; `automation/notifications.py`
sends completion notices.

None of it is rendered. No automation page, no run history, no button for the
trigger. The sidebar still shows a four-step wizard whose step-2 checkmark is
`len(state.selected_clip_ids) > 0` (`ui/app.py:131`), a rule from the old flow.

Auto mode decides seven things (`automation/generation_request.py:108`: memory type,
scope, people, upload, album, plus identity flags) and carries **no** duration,
music, orientation, resolution, transition or title — those fall out of defaults, and
`AutomationConfig` (`config_models_automation.py:35`) has no key for any of them.
`auto run` accepts only `--dry-run/--force/--cooldown/--upload/--quiet`; there is no
way to pin a candidate or run the second suggestion. The scheduled child runs as a
subprocess on a pipe, so it takes the `QuietDisplay` path, and on success its entire
output is discarded (`automation/runner.py:271` keeps tails only on failure).

## 11. Two smaller things

> Re-verified on main `5a1c5ce0`: `cli/__init__.py:67`, `ui/app.py:623` call `configure_logging()` bare; `operations/phases.py:41` still computes `elapsed_seconds` for nobody. Holds.

- **No verbosity control.** `configure_logging()` is called with no arguments from
  both `cli/__init__.py:67` and `ui/app.py:623`. The level is `INFO`, always.
- **Per-phase elapsed is computed and dropped.** `PhaseEvent.elapsed_seconds`
  (`operations/phases.py:41`) is set on every transition and stored nowhere, so
  `runs show` can print only the three phases the run tracker records — its own
  docstring admits analysis and selection are missing.

---

---

# Part 2 — The plan

> **Reassessed 2026-09-12 evening, after v0.77.0 went out.** The decision table above said
> all of this lands before the release. It did not: the release train shipped with none of
> W1–W8 started. Every finding in Part 1 was re-read against `main` at `5a1c5ce0`; all
> eleven hold, four call sites moved by a few lines (noted inline). What changed here is the
> order and the cut into PRs, not the diagnosis. The eight streams stay; they are now split
> into a "before 0.78 is called usable" set (8 PRs, roughly 1,600 lines) and a "0.78.x"
> set. The four decisions stand: one umbrella issue (#824), Suggestions **and** Runs pages,
> chronological storyboard as the default with the ranked story kept, not one prompt byte
> or digest input changed (guards: `tests/test_replay_editorial_routes.py`,
> `tests/test_matrix_routes.py`).

## What the re-read added

Three things the first audit never reached.

**A dead reader is recorded, not reported.** `analysis/llm_query.py:160-170` retries a
transport drop three times (2 s, then 4 s) and raises. The observer hook it offers is wired
only to `analysis/editorial_gateway.py:174` (`record_attempt`), which appends a
`RequestAttemptTrace` to the attempt's record. `analysis/editorial_text_gateway.py:87`
treats only `LLMIncompleteResponse` and `JSONDecisionError` as bounded; a transport error
propagates and the run fails. So the sequence the owner saw ("omlx is not running so
everything is stuck") is: six seconds of silence, then a dead run, and the endpoint name
appears nowhere in either surface. The fix is small and belongs to the progress record
(W1): the observer publishes a `provider_unreachable` stage update naming the endpoint and
its role on the first drop. No prompt changes.

**Clip Review is a per-clip page over the whole selection.** `ui/pages/clip_review.py:256`
renders one row per selected clip with an `ui.expansion` open for the first five (`:283`),
and `:395` `_render_review_selected_clips` iterates every clip with no pagination; the
preview loader at `:39` is created per row. Bulk actions (`:330`) and the quick range
buttons (`:137`) are fine. The page needs the same treatment as the grid: paginate, start a
preview only when its expansion opens, release the video element on close.

**The wizard's person picker is not the problem; Settings → People is.**
`ui/pages/step1_people.py:117` writes into the preset and calls `apply`; no navigation.
`ui/pages/settings_people.py` reloads the browser at `:357`, `:410`, `:446`, fetches one
thumbnail per person (`:137`), and writes the companion file per role change and per
debounced keystroke (`:315` debounce 800 ms → `people/companion.py:112`). That page is a
W6 item, not a W1 one.

**The January grouping.** A story whose grouping went loose presents every carrier it was
granted under one title with nothing showing the days it spans. That is a selection-quality
question, and it cannot be judged from the ranked story view. The storyboard (W5) must
show, per carrier: capture day, the story it was granted to, and the moment id, so the
owner can see "these six pictures span three unrelated days" at a glance. Until that view
exists, grading the grouping means reading `plan.private.json` by hand.

## Before 0.78 is called usable

Eight PRs, in this order. Each is at most ~300 lines, carries its acceptance test, and
names the docs page it owes. All link #824 unless stated.

| # | PR | Files | Test added | Docs owed |
|---|---|---|---|---|
| S1 | `feat(progress): one stage record for both surfaces` (W1) | `operations/cut_progress.py` (a `StageUpdate` with phase, label, done, total, recent ids), `operations/editorial_attempt.py`, `analysis/editorial_runtime.py:525`, `analysis/editorial_projection.py:44-48`, `ui/pages/memory_run.py:165-178`, `cli/_pipeline_runner.py:241-253`, delete the 13 dead keys in `ui/pages/clip_pipeline.py:82` | `tests/test_surface_parity.py`: one fake run, both reporters, same stage sequence and counts | `create/web-ui/memory.mdx` (what the bar means), `create/cli/generate.md` (the progress line) |
| S2 | `feat(progress): name the provider that stopped answering` (W2, part) | `analysis/editorial_gateway.py:174` observer publishes through the S1 record; `analysis/editorial_orchestration.py:166,177,188,204` emit counted updates; surface `selection_stages` from `analysis/editorial_structure_planner.py:520` | a progress test asserting every emitted stage carries counts or an explicit "no unit"; a replay run proving no prompt byte moved | `create/web-ui/memory.mdx` |
| S3 | `fix(ui): one media route, thumbnails not previews` (W4a) | one parameterised, auth-gated media route serving from the session thumbnail cache; grids request `size="thumbnail"`; `<img loading="lazy">` instead of data URIs (`ui/pages/step2_helpers.py:29`, `step2_loading.py:403,429`); route honours demo and privacy blurring | e2e over a large fake library: DOM image count stays bounded while paging; `tests/test_ui_media_routes.py` still passes | `deploy/configuration/network-and-privacy.md` (the route exists, what it serves) |
| S4 | `fix(ui): the grid and the review page stop growing` (W4b) | `ui/pages/clip_grid.py:409` windows the DOM; `:247`, `:310` drop `ui.navigate.to("/step2")`; `clip_review.py:395` paginates and lazy-starts previews; `step4_export.py:72` timer deactivated | e2e: a grid toggle performs no navigation; review page renders N rows for N > page size | none beyond S3 |
| S5 | `fix(ui): a tick survives the cut, and "Cut again" has a pool` (W6a, closes #778) | `ui/pages/memory.py:57` `_recut` re-runs `_set_initial_selection`; `owner_required_asset_ids` beside `owner_excluded_asset_ids` in `analysis/selection_source.py:124`, honoured where `favourite` is (`analysis/editorial_story_carriers.py`); `ui/pages/clip_pipeline.py:363,424` | unit: a required id is in the cut; replay: no prompt byte moved (post-read admission) | `create/web-ui/memory.mdx` (what Include means now) |
| S6 | `feat(ui): the storyboard tab` (W5a) | new `ui/pages/memory_storyboard.py` built from `plan.private.json` carriers and `render-projection.private.json` intervals; default tab; `memory_story_data.py:185` ranked view kept as tab two; per carrier: day, story, moment id, duration, running timecode | storyboard test: shot order equals capture order; timecodes sum to the planned duration | `create/web-ui/memory.mdx` |
| S7 | `fix(ui): Settings → People stops reloading` (W6b) | `ui/pages/settings_people.py:357,410,446` redraw the roster instead of `ui.navigate.reload()`; paginate `:196`; batch thumbnails through the S3 route; coalesce the companion writes at `:315` | e2e: a role change performs no navigation and one write | `create/web-ui/settings.mdx` (the companion editor walkthrough it never had) |
| S8 | `feat(cli): a verbosity flag` (W8, part) | `configure_logging()` takes a level from `cli/__init__.py:67` and `ui/app.py:623` | CLI test: `-v` changes the level | `create/cli/generate.md`, `reference/cli-reference.md` (generated) |

Why this set and this order. S1 is the keystone: nothing about progress can be honest
until the numbers stop dying at `editorial_runtime.py:525`, and S2 needs the record S1
creates. S3 and S4 are the crash: a product that kills a browser tab is not usable, and
S3's route is what S7 batches through. S5 is the oldest user report still open (#778) and
the "Cut again" bug makes the second cut of every session wrong. S6 is the one view that
lets the owner grade selection quality from the UI; without it the January grouping
complaint cannot even be shown. S8 is 40 lines and every other slice is easier to debug
with it. `analysis/editorial_runtime.py` is at 752 lines: S1 adds to it and must stay under
800, or split the reporter out first.

## After 0.78

- **W3 time remaining.** Rate-based estimate within a counted stage (needs S1 counts);
  persist `PhaseEvent.elapsed_seconds` (`operations/phases.py:41`) into the run database
  and estimate from prior runs; fix the export bar (`generate_progress.py:142` maps
  `extract`, `upload`, `done` to the full range) with a test that fails on an unmapped
  phase name. Two PRs.
- **W5b the whole pool with each asset's fate.** `analysis/selection_trace.py` on for every
  run instead of only under `--trace-selection` (`cli/generate_options.py:223`); render "in
  the cut" or "dropped at *stage* because *reason*" against every candidate; `runs story`
  and `runs why` on the CLI; reconcile the two cut keys (`cli/_editorial_context.py`,
  `ui/pages/clip_pipeline.py`). Three PRs.
- **W7 the scheduler's face.** `/suggestions` over `AutoRunner.suggest` with the variety
  rule that rejected each candidate; `/runs` over the `pipeline_runs` table reusing
  `cli/runs.py`; sidebar Memory / Suggestions / Runs / Media pool / Settings; delete the
  stale step-2 rule at `ui/app.py:131`; `auto run --candidate`; keep the successful
  child's output (`automation/runner.py:165` keeps tails only). Three PRs, plus
  `sidebars.ts` for the two pages.
- **W8 rest.** Reader-word mapping (`memory_story_data.py:27`), the deprecated
  `scheduler` group (`create/cli/scheduler.md` still documents it).

## Definition of done: tested in a browser (owner ruling, 2026-09-12)

A stream is done when all three hold, not when CI is green.

1. The hermetic browser run covers it: `make e2e` (fake Immich, fake library and fake editorial
   under `tests/e2e/`) extended with the behaviour the PR adds; `make screenshots` regenerated
   when a page changes.
2. A real Playwright walk-through of the flow the PR touches, on the fixture library: brief, Cut,
   story, export, and every button the PR adds or changes is clicked. For the parity streams the
   same memory runs through the CLI alongside. A GIF or screenshots go in the PR body.
3. A written verdict from the user's seat: does the page make sense for the product use (one person
   making one memory; a scheduler running many), what was confusing, what a first-time self-hoster
   would not understand. The owner grades that verdict before merge.

Before S1 starts, the same walk-through is recorded on 0.77.1 as the baseline, so every later PR
shows before and after.

## Constraints that bound every change

- **Byte-identical replay.** Not one prompt byte, nothing a digest is computed over.
  Guards: `tests/test_replay_editorial_routes.py`, `tests/test_matrix_routes.py`. S2 and
  S5 are the two slices that touch the editorial modules; both run the replay in their PR.
- **No per-asset media routes.** `tests/test_ui_media_routes.py` exists because
  `app.add_media_file` registers a route per call and never removes it.
- **Repo gates**: 800 lines per file, Xenon C, complexipy 15, ~300 lines per PR, one
  concern per PR, a linked issue, conventional commits, squash merge only after the owner
  has tried the slice.
- Docs are owed with every slice, in the table above.

## Verification

| Check | Command | What it proves |
|---|---|---|
| Unit and gates | `make ci` | nothing regressed |
| Hermetic browser | `make e2e` | `tests/e2e/test_memory_page.py` asserts the strip moves and the bar appears |
| Replay | `tests/test_replay_editorial_routes.py`, `tests/test_matrix_routes.py` | no prompt moved |
| Parity | `tests/test_surface_parity.py` (extended in S1) | CLI and UI render the same stages and counts |
| Pipeline | `make test-integration-pipeline` | the CLI surface end to end |
| Docs | `make docs-build` | the pages match the code |

Manual pass after S6: run one memory through the CLI and the same through the UI, diff the
stage sequence, then read the storyboard against the ranked story and say whether the
loose grouping is visible.

## What the owner decides

1. Whether S6 (the storyboard) is a 0.78 blocker. This plan says yes, because it is the
   only way to grade selection quality without reading JSON.
2. Whether 0.78 waits for W7. This plan says no: the scheduler's audience is not blocked,
   the ad-hoc user is.
3. Nothing else: the merge method is settled (squash, with `history/` branches keeping the
   per-commit record when a stack matters), and #834 covers the release trigger.

## Related issues

- **#824** — the umbrella issue; its checklist mirrors the S1–S8 table.
- **#778** — closes with S5.
- **#809** — closed into #824 on 2026-09-12.
- **#834** — release governance; decides the merge method above.
