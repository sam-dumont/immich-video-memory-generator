# uv is the only package pip fetches from PyPI while building the image; every
# other install resolves from /wheels or the uv.lock constraints. Hash-pinning
# it closes the build's last unverified download.
#
# The hashes cover both published arches (the image builds linux/amd64 and
# linux/arm64) plus the sdist fallback — pip accepts whichever artifact it
# resolves as long as it matches one of them. Bumping the version means
# replacing every hash — renovate does it automatically, or read them from
# https://pypi.org/pypi/uv/<version>/json (the linux wheels and the sdist).
uv==0.12.15 \
    --hash=sha256:a673bc39e677eaa012f618cab2e6e99fb82919f37ea45043fca530e9ca94c082 \
    --hash=sha256:aee9802f46bae436bd91751bb33ddeb379ef1596b5c19df193219d545d244b60 \
    --hash=sha256:013e3a5774fb2cc036a9422edc51c8c5273cca011ec04c2817249b8a4a0b0f02 \
    --hash=sha256:5eef1c0d2e0ae26cbe1daa49de855b09dfb63a45eaf1fe63dea2827249eadc05
