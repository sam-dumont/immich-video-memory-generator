# Changelog

This changelog is automatically generated by [python-semantic-release](https://python-semantic-release.readthedocs.io/) on each tagged release.

For detailed changelogs per version, see [GitHub Releases](https://github.com/sam-dumont/immich-video-memory-generator/releases).

## Unreleased

- Retire the standalone `scheduler`, `analyze`, `export-project` and scorer cache commands.
  Use `auto` for automation and the editorial preparation and run commands for current memories.
  Removed config keys, including the ineffective decorative-line setting, warn and are
  discarded when saved.
- Use disk-backed persistent storage for container caches and scratch. Kubernetes app state and
  output claims start at 50 GiB each, with a separate model claim. Terraform removes `tmp_size`.
- Correct the self-hosting manual against current configuration, CLI, selection and rendering code.
- Preserve readiness after a `/health` request; honour `--config` for database backup and the
  configured cache directory in the UI's preview cleanup.
