---
sidebar_position: 0
title: "Memory"
---

import ThemedScreenshot from '@site/src/components/ThemedScreenshot';

# The Memory page

`/` is one page with three states: the **brief**, the **cut** in progress, and what the cut produced. It reads like `immich-memories generate`: one memory type, that type's parameters, one duration, one button. The sidebar calls it **Memory**.

<ThemedScreenshot name="memory-brief" alt="The brief: memory type, its parameters and the duration line" />

## The brief

**Memory type** offers exactly what `generate --memory-type` accepts, in the same order — Year in Review, Season, Person Spotlight, Multi-Person, Monthly Highlights, On This Day, Album, Trip, Holiday, Surprise me — plus **Custom date range**, which the CLI spells as `--start`/`--end`. Picking a type shows only that type's parameters; they are listed under [Memory types and their parameters](./step1-configuration.mdx).

**Duration** is one line. With **Auto duration** on it shows the type's own answer ("Auto · 1m 00s" for a month) and where the number comes from; Trip and Album measure theirs from the media once it is loaded. Turn the switch off to type an exact **Target duration (min)**; fractional minutes keep their seconds.

**Advanced** holds the Immich connection panel (URL, API key, Test Connection, Save Config — see [Settings](./settings.mdx#saving-config-from-the-memory-page-and-what-it-costs-you)), the pool switches the selection route reads (Include Photos, Include Live Photos, Accept Forwarded Media, HDR clips only) and **Open the media pool**, which goes to the [media pool and excerpt editor](./step2-clip-review.mdx).

The brief appears once Immich has answered. With credentials in `config.yaml` or the environment the page connects on its own; otherwise only the connection panel shows until **Test Connection** succeeds.

## Cut

**Cut** loads the pool for the brief, then runs the story-first selection over it. There is no review step in between: everything the brief found is eligible, and the editor decides. To keep something out first, open the media pool from Advanced, untick it, and start the run from that page's own **Generate Memories** button.

While it runs the page shows one row per phase — Finding media, Loading thumbnails, Reading the pictures, Editing, Done — with the run's own stage under the active row ("Reading the period account", "Editing the memory"), the elapsed time, and **Cancel**, which stops after the current stage.

One session runs one cut at a time: **Cut**, **Cut again** and the pool page's **Generate Memories** are refused while a cut is running. The run belongs to the session, not the tab. Reload the page mid-cut and it lands on the same rows: the page polls the attempt the run writes under the cache directory (`editorial-runs/<key>/latest-attempt.private.json`), and when the run finishes the story appears without a second run. A cut that was cancelled, failed, or interrupted — the process that ran it is gone, which the attempt's released lease tells apart from a slow live run — is reported on the page with **Cut again**. A cut that finished while the session held no result for it is read back from the attempt, and **Re-load media to export** fetches the pool again and rebuilds the selection from the attempt's projection so Export works.

## What the cut produced

When the cut finishes the page shows the story the editor told, read from the plan the run wrote:

<ThemedScreenshot name="memory-story" alt="The story: thesis, stories in weight order, and each story's pictures with their reasons" />

- the **thesis** — the editor's one-paragraph reading of the period;
- one **duration line** — how many seconds of pictures and video were selected for the memory asked for, how much of that was available for content, and whether the cut landed near target or fell short;
- the **stories**, heaviest first (dominant, major, minor, glimpse), each with its purpose and how many pictures it was granted;
- under each story its **pictures** in capture order, with a **Still** or **Motion** badge (the final-cut rendering decision, or the source kind when none was made), the seconds it holds, when it was taken, the editor's one-line reason, and its standing (remarkable, maybe).

A cut that left no plan behind shows its counts instead. Below the story, four buttons:

- **Export** — straight to [Preview & Export](./step4-preview-export.mdx). The [options page](./step3-generation-options.mdx) is one click away from there with **Back to Generation Options**.
- **Review the pool** — the media pool with the cut's picks, and the excerpt editor.
- **Cut again** — the editor runs again over the same pool.
- **Change the brief** — back to an empty brief.

:::note
The screenshots on the other Web UI pages and the demo video still show the four-step wizard this page replaces; they are being redone.
:::
