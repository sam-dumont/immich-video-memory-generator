codecov:
  # WHY: Integration coverage XMLs (tests/*-coverage.xml) are generated locally
  # and committed — their timestamps are stale. CI freshens them before upload,
  # but this disables the server-side check as a safety net.
  max_report_age: off

# WHY: Without flag definitions, Codecov auto-creates flags but doesn't
# carry them forward — so a commit touching only unit tests would show 0%
# integration coverage instead of keeping the last known value.
flags:
  unittests:
    paths:
      - src/immich_memories/
    carryforward: true
  integration:
    paths:
      - src/immich_memories/
    carryforward: true
  integration-linux:
    paths:
      - src/immich_memories/
    carryforward: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        # WHY: Patch coverage is enforced locally via `make diff-cover` (80%
        # threshold with merged integration XMLs). Codecov only sees CI unit
        # tests, so it under-counts coverage for code that needs FFmpeg/Immich.
        # Making this informational avoids false failures on PRs.
        informational: true
